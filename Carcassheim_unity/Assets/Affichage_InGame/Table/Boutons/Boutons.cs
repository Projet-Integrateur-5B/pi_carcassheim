using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/* 
/* public enum Button
{
        Cancel,
        LeftArrow,
        RightArrow,
        Validate
}; */

public class Boutons : MonoBehaviour
{
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }

        /*       public void display()
              {
                      model_renderer.enabled = true;
                      if (state == TileIndicatorState.TilePossibilitie)
                              tile_collider.enabled = true;
              }


              public void hide()
              {
                      model_renderer.enabled = false;
                      if (state == TileIndicatorState.TilePossibilitie)
                              tile_collider.enabled = false;
              }

              // Si la position de la tuile est nulle, on n'affiche pas le résultat
              //  */
}